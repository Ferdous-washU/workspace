This repository contains a docker container to setup and run MariaDB in Codespaces.
On creating a codespace with this repo, MariaDB, SQLTools and Python are installed during initialization. 
A startup script creates a MariaDB user is created along with a sample database.
